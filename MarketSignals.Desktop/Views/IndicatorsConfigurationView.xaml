<UserControl
    x:Class="MarketSignals.Desktop.Views.IndicatorsConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MarketSignals.Desktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <ComboBox ItemsSource="{Binding IndicatorNames}" SelectedValue="{Binding SelectedIndicator}" />
            <ComboBox ItemsSource="{Binding PairNames}" SelectedValue="{Binding SelectedPair}" />
            <ComboBox ItemsSource="{Binding Intervals}" SelectedValue="{Binding SelectedInverval}" />
            <Button Command="{Binding SendRequestCommand}" Content="Send" />
            <TextBlock Text="{Binding ReturnedValue}" />
        </StackPanel>
    </Grid>
</UserControl>
