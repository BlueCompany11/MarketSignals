<UserControl
    x:Class="MarketSignals.Desktop.Views.SignalView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MarketSignals.Desktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <WrapPanel>
            <TextBlock
                Margin="0,0,20,0"
                FontWeight="DemiBold"
                Foreground="Gray"
                Text="{Binding From}" />
            <TextBlock
                FontWeight="DemiBold"
                Foreground="Gray"
                Text="{Binding Date}" />
        </WrapPanel>
        <WrapPanel Grid.Row="1">
            <TextBlock
                Margin="0,5,0,10"
                FontSize="24"
                FontWeight="ExtraBlack"
                Text="{Binding Text}" />
        </WrapPanel>
        <TextBox
            Grid.Row="2"
            Background="Transparent"
            BorderThickness="0"
            IsReadOnly="True"
            Text="{Binding Url, Mode=OneWay}"
            TextWrapping="Wrap" />
    </Grid>
</UserControl>
